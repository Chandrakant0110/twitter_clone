import 'package:flutter/material.dart';

class GrokView extends StatelessWidget {
  const GrokView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Grok Page.'));
  }
}
